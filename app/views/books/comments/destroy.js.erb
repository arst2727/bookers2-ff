<!--class="bookcomにをrenderで指定した部分テンプレートに更新-->
$(".bookcom_<%= @book_comment.book_id %>").html("<%= j(render 'books/comment', book: @book, comment: @book_comment) %>");
<!--フォームを送信したあとにフォームの値を空にします-->
$("textarea").val('');